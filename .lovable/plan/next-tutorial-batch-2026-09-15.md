# Next Tutorial Batch

## Goal
Extend the AmmarAI tutorial center with the next verified end-user workflows while preserving the existing Neo-Swiss presentation and strict screenshot-branding rules.

## Scope
- Audit the next reference documentation candidates against AmmarAI’s live tool pages and retain only workflows that genuinely exist in both products.
- Build the next 3–5 distinct tutorials; exclude duplicates, administration, provider configuration, API keys, and unsupported steps.
- Write original AmmarAI instructions in the reference workflow order, with top and bottom “Try [Tool] in AmmarAI →” links.
- Use full-resolution reference workflow screenshots only. Replace the source logo and far-right avatar with the AmmarAI logo, measuring each original mark separately so every replacement matches its exact center and footprint.
- Add each guide to tutorial search, categories, related/previous/next navigation, metadata, structured data, sitemap, and the tutorial audit map.
- Verify every guide on desktop and mobile: correct branding, sharp images, working links, alt text, no overflow, and no source/admin terminology.

## Technical details
- Extend the existing tutorial data model and route presentation rather than introducing a second documentation system.
- Keep native screenshot dimensions and record per-image replacement bounds in the tutorial audit map.
- Run focused type checks, build diagnostics, and browser checks across the new pages and tutorial hub.
